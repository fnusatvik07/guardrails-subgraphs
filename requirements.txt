# Pinned to the versions these notebooks were written and tested against.
# Loosen them once you know your own environment behaves.
langchain==1.3.18
langchain-core==1.6.1
langchain-openai==1.6.0
langgraph==1.2.11
langgraph-checkpoint==4.2.0
python-dotenv>=1.0
jupyterlab>=4.4
grandalf>=0.8          # ASCII graph drawing for cr.draw()
cairosvg>=2.7          # only needed to re-render the diagrams as PNG
